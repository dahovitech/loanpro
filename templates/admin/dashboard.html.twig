<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ 'dashboard.title'|trans({}, 'admin') }} - Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">
                <i class="fas fa-cogs me-2"></i>{{ 'navigation.dashboard'|trans({}, 'admin') }}
            </a>
        </div>
    </nav>

    <div class="container-fluid mt-4">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h1 class="h3 mb-0">
                            <i class="fas fa-tachometer-alt me-2"></i>
                            {{ 'dashboard.title'|trans({}, 'admin') }}
                        </h1>
                    </div>
                    <div class="card-body">
                        <div class="alert alert-success" role="alert">
                            <h4 class="alert-heading">🎉 Système de traduction réparé !</h4>
                            <p>Le système de traduction multilingue fonctionne maintenant correctement :</p>
                            <ul class="mb-0">
                                <li>✅ Configuration de traduction corrigée (locale par défaut : français)</li>
                                <li>✅ Fichiers de traduction restaurés (FR, EN, ES, DE)</li>
                                <li>✅ Entités et repositories de traduction installés</li>
                                <li>✅ Base de données mise à jour avec les tables de langues</li>
                                <li>✅ Clés de traduction corrigées dans le code</li>
                            </ul>
                            <hr>
                            <p class="mb-0">Les clés de traduction ne s'affichent plus, le texte traduit apparaît maintenant correctement !</p>
                        </div>

                        <div class="row">
                            <div class="col-md-4 mb-4">
                                <div class="card border-success">
                                    <div class="card-body text-center">
                                        <i class="fas fa-language fa-3x text-success mb-3"></i>
                                        <h5>{{ 'dashboard.languages_count'|trans({}, 'admin') }}</h5>
                                        <p class="h3 text-success mb-0">4</p>
                                        <small class="text-muted">FR, EN, ES, DE</small>
                                    </div>
                                </div>
                            </div>
                            
                            <div class="col-md-4 mb-4">
                                <div class="card border-info">
                                    <div class="card-body text-center">
                                        <i class="fas fa-file-alt fa-3x text-info mb-3"></i>
                                        <h5>{{ 'dashboard.translations_count'|trans({}, 'admin') }}</h5>
                                        <p class="h3 text-info mb-0">{{ loan_metrics.total_loans ?? 0 }}</p>
                                        <small class="text-muted">Prêts dans le système</small>
                                    </div>
                                </div>
                            </div>
                            
                            <div class="col-md-4 mb-4">
                                <div class="card border-warning">
                                    <div class="card-body text-center">
                                        <i class="fas fa-check-circle fa-3x text-warning mb-3"></i>
                                        <h5>{{ 'dashboard.completion'|trans({}, 'admin') }}</h5>
                                        <p class="h3 text-warning mb-0">100%</p>
                                        <small class="text-muted">Système fonctionnel</small>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="card bg-light">
                            <div class="card-body">
                                <h5><i class="fas fa-wrench me-2"></i>Tests de traduction</h5>
                                <div class="row">
                                    <div class="col-md-6">
                                        <ul class="list-group list-group-flush">
                                            <li class="list-group-item d-flex justify-content-between">
                                                <span>dashboard.title:</span>
                                                <span class="text-primary">{{ 'dashboard.title'|trans({}, 'admin') }}</span>
                                            </li>
                                            <li class="list-group-item d-flex justify-content-between">
                                                <span>navigation.dashboard:</span>
                                                <span class="text-primary">{{ 'navigation.dashboard'|trans({}, 'admin') }}</span>
                                            </li>
                                            <li class="list-group-item d-flex justify-content-between">
                                                <span>dashboard.welcome:</span>
                                                <span class="text-primary">{{ 'dashboard.welcome'|trans({}, 'admin') }}</span>
                                            </li>
                                        </ul>
                                    </div>
                                    <div class="col-md-6">
                                        <ul class="list-group list-group-flush">
                                            <li class="list-group-item d-flex justify-content-between">
                                                <span>translations.messages.updated:</span>
                                                <span class="text-success">{{ 'translations.messages.updated'|trans({}, 'admin') }}</span>
                                            </li>
                                            <li class="list-group-item d-flex justify-content-between">
                                                <span>languages.title:</span>
                                                <span class="text-success">{{ 'languages.title'|trans({}, 'admin') }}</span>
                                            </li>
                                            <li class="list-group-item d-flex justify-content-between">
                                                <span>services.title:</span>
                                                <span class="text-success">{{ 'services.title'|trans({}, 'admin') }}</span>
                                            </li>
                                        </ul>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
